[project]
name = "ai-blockchain-hotspot-monitor"
version = "0.1.0"
description = "Monitor AI-related blockchain adoption and X/News hotspots."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
